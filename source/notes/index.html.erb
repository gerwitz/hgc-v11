---
title: Notes
layout: site
noindex: true
blog: notes
pageable: true
per_page: 12
---

<% if paginate && prev_page %>
<div class="pagination">
<!--  <p>Page <%= page_number %> of <%= num_pages %></p> -->
  <span class="prev"><%= link_to '← Newer', prev_page %></span>
</div>
<% end %>

<ol class="notes-list h-feed">
  <% page_articles.each_with_index do |note, i| %>
    <% next if note.body.empty? %>
    <li>
      <div class="note h-entry">
        <div class="content e-content">
          <%= note.body %>
        </div>
        <div class="meta">
          <% link_to note do %>
            <time class="date dt-published" datetime="<%= note.date.strftime("%F") %>">&mdash;&nbsp;<%= note.date.strftime('%B %e, %Y, %k:%M') %></time>
          <% end %>
        </div>
      </div>
    </li>
    <% end %>
</ol>

<% if paginate && next_page %>
<div class="pagination">
  <span class="next"><%= link_to 'Older →', next_page %></span>
</div>
<% end %>

sudo: false
language: ruby
cache: bundler
rvm:
- 2.2.4
# before_install:
# - gem update --system
# - gem update bundler
script:
- bundle exec middleman build
after_success:
- bundle exec s3_website push
